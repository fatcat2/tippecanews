dist: xenial
language: python
python:
    - "3.7"

before_install:
    - sudo add-apt-repository ppa:deadsnakes/ppa -y
    - sudo apt-get update

install:
    - pip install -r requirements.txt
    - pip install nox

script:
    - nox
